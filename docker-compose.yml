
services:
  ai-sandbox:
    image: ai-sandbox
    container_name: ai-sandbox
    stdin_open: true
    tty: true
    ports:
      - "6420:6420"  # backlog.md browser interface
    volumes:
      - ${SANDBOX_WORKSPACE}/workspace:/workspace
      - ${SANDBOX_WORKSPACE}/ai-cli-data/:/home/aiuser/
    working_dir: /workspace
    environment:
      - UV_PROJECT_ENVIRONMENT=.venv-docker
      - UV_LINK_MODE=copy

    env_file:
      - .env
